# Basic App

A minimal PyReact Start example — single page, no database.

## Setup

```bash
pip install vegabase
bun install
```

## Development

```bash
# Terminal 1
vegabase dev

# Terminal 2
export APP_ENV=development
python -m backend.main
```

Visit `http://localhost:8000`
