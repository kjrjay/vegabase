[project]
name = "{{project_name}}"
version = "0.1.0"
description = "A PyReact Start app with posts example"
requires-python = ">=3.11"
dependencies = [
    "pyreact-start",
    "fastapi>=0.115.8",
    "uvicorn>=0.34.0",
]

[project.optional-dependencies]
postgres = ["psycopg[binary]>=3.0.0"]
